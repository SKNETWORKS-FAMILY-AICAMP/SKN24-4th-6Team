# Django
DJANGO_SECRET_KEY=change-me-use-get_random_secret_key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# aigo-ai (FastAPI RAG service) base URL — chat proxy posts here.
AIGO_AI_BASE_URL=https://l2yx2nnbwyspte-8000.proxy.runpod.net/v1

# PostgreSQL — 로컬은 SQLite 사용하므로 비워둠
DB_NAME=aigo_web
DB_USER=aigo
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_PORT=5432

# Gmail SMTP — 로컬은 콘솔 출력이므로 비워둠
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

QDRANT_PATH=
QDRANT_URL=
QDRANT_API_KEY=
COLLECTION_LEGAL=
COLLECTION_CONTRACTS=